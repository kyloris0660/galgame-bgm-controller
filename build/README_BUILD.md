# Build & Distribution (Windows)

Double-click `build\build_gui.bat` from the repo root.
Outputs:
- `dist\GalBGMController\GalBGMController.exe` (one-dir)
- `dist\GalBGMController-single.exe` (one-file)

Icon: `assets/app.ico` (generated from your PNG).
